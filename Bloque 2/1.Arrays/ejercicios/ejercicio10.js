// Crea un array llamado letras que contenga las letras 'a', 'b', 'c', 'd'. Usa el método reverse para invertir el orden de los elementos y muestra el array invertido en la consola.

let letras = ['a', 'b', 'c', 'd'];

let letrasReverse = letras.reverse();

console.log(letrasReverse);