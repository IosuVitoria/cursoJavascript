// DIFICULTAD EXTRA: Utilizando un bucle for, recorre el array colores e imprime cada color en la consola. 
// Hasta el momento no hemos visto la estructura for. Este ejercicio sirve para aquellos que conozcan esta estructura antes de empezar el libro. 
// Si no lo habeis visto se puede pasar por alto.

let colores = ["azul", "rojo", "amarillo", "verde"];

for(let i = 0; i < colores.length; i++){
    console.log(colores[i]);
}