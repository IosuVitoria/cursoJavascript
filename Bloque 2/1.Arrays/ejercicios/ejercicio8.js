// Crea un array llamado numeros que contenga los números del 1 al 5. Usa el método indexOf para encontrar la posición del número 3 en el array y muestra el resultado en la consola.

let numeros = [1, 2, 3, 4, 5];

console.log(numeros.indexOf(3));