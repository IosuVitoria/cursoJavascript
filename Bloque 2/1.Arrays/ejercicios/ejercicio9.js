// Usando el array numeros, comprueba si el número 6 está en el array utilizando el método includes. Muestra true o false en la consola.

let numeros = [1, 2, 3, 4, 5];

console.log(numeros.includes(6));