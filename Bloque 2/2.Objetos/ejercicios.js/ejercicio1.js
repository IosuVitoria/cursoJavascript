// 1.	Crea un objeto "persona" con las propiedades nombre, edad, y profesión. Luego, imprime cada una de estas propiedades en la consola.

const persona = {
    nombre: "Iosu",
    edad: 38,
    profesion: "programador"
}

console.log(`Me llamo ${persona.nombre}.`);
console.log(`Tengo ${persona.edad} años.`);
console.log(`Soy ${persona.profesion}.`);