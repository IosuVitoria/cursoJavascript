// Declara una variable contador y asígnale el valor 0.
// Incrementa la variable contador en 1 y asígnala a una nueva variable contadorIncrementado.
// Decrementa la variable contador en 1 y asígnala a una nueva variable contadorDecrementado.

let contador = 0;
let contadorIncrementado = ++contador;

contador = 0;
let contadorDecrementado = --contador;

contador = 0;
console.log(contadorDecrementado, contador, contadorIncrementado);

// NOTA: Juega con la posición de la variable contador y los operadores poniéndolos delante o detrás para ver los
// cambios que se producen en el console.log de la linea 12.